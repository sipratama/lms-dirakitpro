# Playbook Bootstrap Project

> Dibuat: 2026-08-27
> Tujuan: dokumen ini mencatat urutan langkah dari nol yang dipakai untuk membangun DirakitPro, sekaligus dijadikan **boilerplate checklist** untuk project berikutnya. Kolom "Status di DirakitPro" adalah contoh nyata pemakaian; untuk project baru, salin daftar langkah lalu reset semua status ke Pending.

## Cara pakai dokumen ini

1. Saat mulai project baru, copy isi bagian "Checklist langkah" ke repo baru, reset kolom status.
2. Setiap kali satu langkah selesai, update status dan tambahkan tautan ke file/commit yang jadi bukti langkah itu selesai.
3. Kalau di tengah jalan menemukan langkah baru yang generik (bukan spesifik satu project), tambahkan ke checklist ini juga supaya playbook makin lengkap dari waktu ke waktu.

## Prinsip urutan

- **Dokumen dulu, kode belakangan.** PRD → Data Model → Frontend Design harus selesai (atau minimal stabil sebagai draft-untuk-review) sebelum scaffold aplikasi dimulai, supaya keputusan desain/skema tidak dibuat diam-diam di dalam kode.
- **Git connect di awal, bukan di akhir.** Idealnya `git init` + connect remote dilakukan sesegera folder project dibuat (langkah 0), sebelum PRD/data model/frontend design ditulis, supaya draft dokumen ikut punya riwayat commit. Di DirakitPro langkah git dilakukan belakangan (setelah dokumen selesai) dan itu berhasil, tapi historinya jadi satu commit besar "initialize project" alih-alih riwayat bertahap per dokumen. Untuk project berikutnya, rekomendasi: pindahkan "connect git" ke langkah 0.
- **README dan playbook ini adalah living document.** Update setiap kali status project berubah signifikan (misal scaffold selesai, fitur P0 pertama selesai, dst), jangan ditulis sekali lalu dibiarkan basi.

## Alur kerja branch per langkah

Ditetapkan mulai langkah 6 (Scaffold project) dan berlaku untuk semua langkah setelahnya, supaya riwayat pengembangan tercatat rapi satu branch per unit kerja:

1. **Mulai dari `main` yang bersih.** Setiap siklus kerja baru dimulai dengan `main` lokal yang sudah sinkron dengan remote (`git checkout main && git pull`) — bukan melanjutkan dari branch kerja sebelumnya.
2. **Buat branch baru dulu, baru kerjakan task.** Branch dibuat dari `main` sebelum satu baris kode pun ditulis, dengan penamaan `feat/<nama-singkat-langkah>` (contoh: `feat/scaffold-project`). Satu branch = satu langkah/task di checklist, tidak digabung.
3. **Commit dilakukan di branch tersebut, tanpa push.** Assistant boleh membuat commit lokal sebagai bagian dari menyelesaikan task, tapi **push dan pembuatan PR ke `main` selalu dilakukan oleh user**, bukan assistant.
4. **Assistant wajib melaporkan status selesai/belum secara eksplisit**, dicek terhadap `docs/PROJECT-PLAYBOOK.md`: setelah satu langkah kelar dan lolos verifikasi (lint/typecheck/build/dst), assistant menyatakan langkah mana yang selesai dan langkah mana berikutnya yang bisa dikerjakan — bukan otomatis lanjut mengerjakan langkah berikutnya tanpa konfirmasi user.
5. **Siklus berulang setelah PR di-merge.** Begitu user selesai PR dan merge ke `main`, siklus kembali ke langkah 1 (checkout `main`, pull, branch baru) untuk task berikutnya.

## Checklist langkah

| # | Langkah | Deskripsi / rekomendasi | Output/artifact | Status di DirakitPro |
|---|---|---|---|---|
| 0 | Buat repo kosong + connect git | Buat folder project, `git init`, buat repo remote (GitHub) kosong, `git remote add origin`, lakukan commit pertama sesegera mungkin. Lakukan ini **sebelum** menulis dokumen, bukan setelahnya. | Repo lokal + remote terhubung | Selesai — remote `github.com/sipratama/lms-dirakitpro` terhubung, riwayat berupa 1 commit `initialize project` (dilakukan belakangan setelah dokumen, bukan di awal) |
| 1 | Install skill AI coding agent untuk desain frontend | Install tool/skill yang memberi AI coding agent pedoman desain supaya UI yang nanti dihasilkan tidak "generik AI" (gradient ungu khas AI, card seragam, dst). Untuk DirakitPro dipakai dua: **Impeccable** (`npx impeccable install` lalu `/impeccable init`) dan **Taste Skill** (`npx skills add <repo> --skill "<nama-skill>"`). | `docs/IMPECCABLE.md`, `docs/TASTE-SKILL.md`, folder skill di `.agents/skills/` | Selesai |
| 2 | Bikin PRD | Tulis Product Requirements Document: executive summary, problem & opportunity, target market/persona, positioning & brand vocabulary, goals/non-goals/success metrics, feature spec per modul (beri kode fitur seperti `LRN-005`, `CHK-002` supaya mudah dirujuk lintas dokumen), non-functional requirements, dan rekomendasi tech stack. Buat versi Bahasa Indonesia untuk konteks bisnis + versi English sebagai acuan teknis kalau tim/AI agent butuh presisi istilah. | `DirakitPro_MVP_PRD_Text_First_V1.4.md` (+ `_EN.md`) | Selesai (V1.4) |
| 3 | Bikin Data Model | Turunkan skema database dari PRD: daftar entity & ERD, strategi ID, konvensi naming/timestamp, kebijakan soft delete, dan tandai eksplisit bagian mana yang masih asumsi/keputusan terbuka. Belum perlu migration file nyata di tahap ini, cukup draft untuk direview. | `docs/DATA-MODEL.md` | Selesai (status draft untuk review, belum ada migration file) |
| 4 | Bikin Frontend Design | Turunkan arah desain dari PRD + data model + skill desain (langkah 1): satu kalimat arah desain, kepribadian produk, daftar larangan visual ("anti-slop ban list"), prinsip UX, information architecture/route map, dan bahasa antarmuka. Dokumen ini jadi kontrak yang wajib dipatuhi saat implementasi UI nanti. | `docs/FRONTEND-DESIGN.md` | Selesai |
| 5 | Bikin README | Tulis README yang mendeskripsikan project secara akurat berdasarkan kode/dokumen yang benar-benar ada (bukan aspirasi) — termasuk secara eksplisit menyatakan status repo kalau belum ada kode aplikasi. Update lagi begitu scaffold project mulai dibuat. | `README.md` | Selesai (perlu direvisi ulang begitu scaffold aplikasi mulai ada) |
| 6 | Scaffold project | Inisialisasi aplikasi sesuai stack di PRD §14: pnpm + Turborepo, Next.js (App Router) + TypeScript strict, Tailwind CSS + shadcn/ui, struktur folder route group sesuai `docs/FRONTEND-DESIGN.md` §3. | `package.json`, `app/`, konfigurasi build | Selesai — dikerjakan di branch `feat/scaffold-project`. Monorepo `apps/web` (Next.js 16, TS strict, Tailwind, shadcn/ui) dibuat via `create-next-app` + `shadcn init`; seluruh 25 halaman + 3 route handler di `docs/FRONTEND-DESIGN.md` §3.1 dibuat sebagai placeholder (bukan UI final) dengan `layout.tsx` per shell (`(public)`/`(learner)`/`(admin)`); `pnpm build`, `pnpm lint`, `pnpm typecheck` semua lolos. Belum: shell nyata sesuai §7 PRD, database, auth, payment — menyusul di langkah 7 dst |
| 7 | Setup tooling dasar | ESLint + Prettier, TypeScript strict config, `.env.example` untuk semua secret/variabel (Clerk, Midtrans, R2, Resend, PostHog, Sentry, database URL), husky/lint-staged kalau dipakai. Diperluas atas permintaan user: Dockerfile (`apps/web`, standalone build) + `docker-compose.yml` (Postgres only, dev DB) — app Next.js tetap jalan native via `pnpm dev`, bukan di container. | Config file di root | Selesai — dikerjakan di branch `feat/tooling-and-database-setup`. Prettier + `.prettierrc.json`/`.prettierignore` ditambah (root sudah diformat ulang, `pnpm format:check` bersih); husky v9 (`pre-commit` → lint-staged, `pre-push` → typecheck+lint) + `.lintstagedrc.json`; `.env.example` berisi kredensial DB (dipakai `docker-compose.yml`) + placeholder Clerk/Midtrans/R2/Resend/PostHog/Sentry untuk langkah 9–11; `docker-compose.yml` (Postgres 17) diverifikasi jalan & connect (`pg_isready` + query test); `apps/web/Dockerfile` (multi-stage, `output: standalone`) diverifikasi build + run + serve 200 lewat `docker build`/`docker run` manual. **Catatan:** integrasi `eslint-config-prettier` ke `apps/web/eslint.config.mjs` diblokir oleh hook config-protection repo ini (tidak boleh mengubah file config linter) — dilewati, tidak krusial karena `eslint-config-next` tidak punya rule stylistic yang bentrok dengan Prettier saat ini. |
| 8 | Setup database | Init Drizzle ORM, generate migration awal dari `docs/DATA-MODEL.md`, jalankan migration ke PostgreSQL (lokal/dev), setup trigger `updated_at` sesuai keputusan di data model. | Folder `drizzle/`, koneksi DB dev jalan | Selesai — dikerjakan di branch `feat/database-drizzle-setup`. Skema Drizzle ditulis dipecah per domain (`apps/web/db/schema/*.ts`: enums, identity, media, catalog, checkpoint, milestone, enrollment, commerce, project, feedback, lesson-content) mengikuti `docs/DATA-MODEL.md` §3 persis — 22 tabel, 21 enum, CHECK constraint, partial unique index. `LessonContentBlock` didefinisikan sebagai Zod discriminated union (`schema/lesson-content.ts`) sesuai catatan implementasi di dokumen. Migration awal (`drizzle/0000_...sql`) + migration custom trigger `set_updated_at()` (`drizzle/0001_updated_at_triggers.sql`, 13 tabel sesuai §1.4, `orders` dikecualikan) sudah di-generate dan **di-`migrate` ke Postgres lokal (docker-compose) dan diverifikasi fungsional**: trigger `updated_at` teruji jalan, CHECK constraint `courses_price_consistency` teruji menolak data tidak valid. `db/client.ts` (Drizzle + `postgres-js`) baca `DATABASE_URL` dari `.env` root; `next.config.ts`/`drizzle.config.ts` di-guard supaya tidak crash kalau `.env` tidak ada (mis. saat build di dalam Docker image). Catatan: `courses.thumbnailAssetId` ↔ `mediaAssets.courseId` circular reference diselesaikan dengan pola resmi Drizzle (`AnyPgColumn` cast). |
| 9 | Setup auth | Integrasi Clerk, buat mapping `users` ↔ `auth_identities` sesuai data model, middleware role-based (`LEARNER`/`ADMIN`) untuk route group learner/admin. | Middleware auth jalan di 3 shell (public/learner/admin) | Selesai — dikerjakan di branch `feat/clerk-auth-setup`. `apps/web/proxy.ts` (nama file baru Next.js 16 pengganti `middleware.ts`, sudah dicek ke dokumentasi resmi) cuma menyalakan `clerkMiddleware()` untuk konteks auth global. Proteksi route pakai **resource-based auth check** (rekomendasi resmi Clerk, bukan `createRouteMatcher` yang deprecated): `auth.protect()` langsung di `app/(learner)/layout.tsx` (redirect ke sign-in kalau belum login) dan `app/(admin)/layout.tsx` (`auth.protect()` + cek role `ADMIN` via `lib/auth.ts#getUserRoleByClerkId`, `notFound()` kalau bukan admin). `(public)` sengaja tidak digerbang sama sekali, sesuai kontrak scaffold di `docs/FRONTEND-DESIGN.md`. Webhook `/api/webhooks/clerk` diimplementasi penuh: verifikasi signature (`verifyWebhook` bawaan `@clerk/nextjs/webhooks`), upsert idempoten `users`+`auth_identities` untuk `user.created`/`user.updated`, soft-delete `users.deletedAt` untuk `user.deleted` — persis alur di `docs/DATA-MODEL.md` §3.2.1. `ClerkProvider` dipasang di root layout. Bug ditemukan & diperbaiki sekalian: `db/client.ts` sebelumnya `throw` eager kalau `DATABASE_URL` kosong, yang bikin `next build` gagal di dalam Docker (langkah 7) karena `.env` sengaja tidak ada di image — diperbaiki jadi lazy. **Diverifikasi runtime penuh dengan Clerk key asli**: `pnpm typecheck`/`lint`/`build` (host+Docker) lolos; `/` dan `/courses` 200 (publik tidak digerbang); `/dashboard` dan `/admin` 307 redirect ke halaman sign-in Clerk sungguhan (belum login → redirect, bukan role-check dulu, sesuai perilaku resmi `auth.protect()`); webhook Clerk menolak request tanpa signature valid (400). Tidak ada utang teknis tersisa dari langkah ini. |
| 10 | Setup pembayaran | Integrasi Midtrans Snap (sandbox dulu) + endpoint webhook, pastikan idempotency sesuai PRD §COM-005. | Flow checkout sandbox berhasil | Selesai (kode+logika) — dikerjakan di branch `feat/midtrans-payment-setup`. `apps/web/lib/checkout.ts#createCheckoutOrder` implementasi COM-001/002/006/007 (course harus PUBLISHED+berbayar, blokir kalau sudah enrolled ACTIVE/COMPLETED, reuse/redirect ke order PENDING yang masih valid alih-alih bikin duplikat, sweep lazy order PENDING yang sudah lewat `expiresAt` jadi EXPIRED sebelum bikin order baru). `apps/web/lib/midtrans.ts`: `createSnapTransaction` (COM-003, credential server-only, `callbacks.finish` balik ke `/payment/[orderId]`), `verifyMidtransSignature` (SHA512 + `timingSafeEqual`), `mapMidtransStatus` (transaction_status+fraud_status Midtrans → enum internal `payments.status`). Checkout page (`(public)/checkout/course/[courseSlug]`) pakai resource-based `auth.protect()` (pola sama seperti langkah 9) lalu render Snap **overlay** client-side (`components/snap-checkout.tsx`, sesuai `docs/FRONTEND-DESIGN.md` §4.1 — bukan redirect ke hosted page) yang callback-nya cuma navigasi ke `/payment/[orderId]`, tidak pernah memberi akses langsung dari browser (COM-004). Halaman `/payment/[orderId]` menampilkan status dari DB (bukan dari browser callback), auto-poll (`components/payment-status-poller.tsx`) selagi PENDING, dan dibatasi hanya pemilik order (privasi). Webhook `/api/webhooks/midtrans` diimplementasi penuh: verifikasi signature wajib sebelum proses apa pun, cari `payments` by `providerOrderId`, lalu dalam satu `db.transaction` update `payments` + transisi `orders` (PENDING→PAID/EXPIRED/CANCELLED sesuai state machine PRD §10.1) + `enrollments` di-upsert `ON CONFLICT (user_id, course_id) DO NOTHING` (COM-005, idempotent terhadap webhook retry). Refund (Midtrans `refund`/`partial_refund`) sengaja belum memutasi `orders`/`refunds` — hanya dicatat di `payments` untuk audit; alur refund admin penuh ditunda ke langkah 14 (di luar scope "checkout+webhook" langkah ini). `pnpm typecheck`/`lint`/`build`/`format:check` (host) dan `docker build` semua lolos; verifikasi manual: `/checkout/course/[slug]` dan `/payment/[orderId]` benar ter-redirect ke sign-in kalau belum login (resource-based check jalan), webhook menolak signature salah (400) dan menerima signature valid dengan graceful no-op untuk order tak dikenal (200, tidak retry-storm). **Belum diverifikasi**: flow checkout sungguhan lewat Midtrans Sandbox — `MIDTRANS_SERVER_KEY`/`NEXT_PUBLIC_MIDTRANS_CLIENT_KEY` di `.env` user masih kosong per 2026-08-27 (belum diisi, keputusan user: nanti saja, tidak menghalangi lanjut ke langkah berikutnya), perlu diisi manual dari Midtrans Sandbox Dashboard sebelum verifikasi end-to-end (bayar pakai kartu test sandbox → webhook asli masuk → enrollment aktif) bisa dilakukan. |
| 11 | Setup layanan pendukung | Cloudflare R2 (image storage), Resend + React Email (transactional email), PostHog (analytics), Sentry (monitoring) — boleh dicicil sesuai fitur mana yang sedang dikerjakan, tidak harus sekaligus di awal. | Env terkonfigurasi, smoke test tiap layanan | **Dilewati untuk sementara** (keputusan user, 2026-08-27) — belum ada fitur yang butuh R2/Resend/PostHog/Sentry sekarang, jadi ditunda dan boleh dicicil belakangan sesuai fitur mana yang sedang dikerjakan (lihat deskripsi kolom kedua). Lanjut ke langkah 12 dulu. |
| 12 | Setup CI | GitHub Actions untuk lint, typecheck, dan test otomatis di setiap PR/push. | `.github/workflows/*.yml` | Selesai — dikerjakan di branch `feat/ci-setup`. `.github/workflows/ci.yml`: satu job `verify` (ubuntu-latest) jalan di setiap `push`/`pull_request` ke `main` — `actions/checkout` → `pnpm/action-setup` (baca versi pnpm dari `packageManager` di `package.json`, tidak di-hardcode dobel) → `actions/setup-node` (Node 22 sesuai `engines`, cache pnpm) → `pnpm install --frozen-lockfile` → `pnpm typecheck` → `pnpm lint` → `pnpm format:check` → `pnpm build`. Build tidak butuh secret apa pun (Clerk/Midtrans/DB) karena semua koneksi env-dependent bersifat lazy — sudah diverifikasi manual sejak langkah 8–10 kalau `next build`/`docker build` jalan tanpa `.env`. **Catatan:** job `test` belum ada karena belum ada test suite (menyusul di langkah 13) — tinggal tambah satu step `pnpm test` begitu langkah 13 selesai. Branch protection rule ("wajib CI hijau sebelum merge") belum diaktifkan di GitHub — itu ubahan setting repo bersama, sengaja tidak dilakukan otomatis, perlu keputusan/aksi eksplisit user di GitHub kalau mau. **Bug ditemukan & diperbaiki sekalian**: `pnpm typecheck` gagal di CI (`Cannot find name 'LayoutProps'` di `app/layout.tsx`) meski lolos di lokal — root cause: `LayoutProps` adalah type hasil generate `next typegen`/`next build`/`next dev` yang disimpan di `apps/web/.next/types` (folder gitignored); lokal selalu ada sisa `.next/` dari run sebelumnya jadi kebetulan lolos, checkout bersih CI tidak punya itu sama sekali. Diperbaiki dengan mengubah script `typecheck` di `apps/web/package.json` jadi `next typegen && tsc --noEmit` — diverifikasi ulang dari kondisi bersih (`rm -rf apps/web/.next` lalu `pnpm typecheck`) berhasil, plus `lint`/`format:check`/`build` tetap hijau. |
| 13 | Setup testing | Vitest + React Testing Library (unit/komponen), Playwright (E2E) untuk alur inti (`Discover → Enroll/Purchase → Lesson → Checkpoint → Submission`). | Test suite awal + script `test` di `package.json` | Selesai — dikerjakan di branch `feat/setup-testing`. **Vitest + RTL**: `apps/web/vitest.config.mts` (jsdom, alias `@/*`), `apps/web/lib/midtrans.test.ts` (12 test, semua cabang `mapMidtransStatus` + `verifyMidtransSignature`), `apps/web/components/payment-status-poller.test.tsx` (3 test RTL). Script `"test": "vitest run"` di `apps/web/package.json`, diwire ke `pnpm test` root lewat task `test` baru di `turbo.json`. 18/18 test lolos. **Playwright E2E**: `apps/web/playwright.config.ts` + direktori `apps/web/e2e/` (dua project: `anonymous` dan `authenticated`), tidak diwire ke `turbo.json` (sengaja — E2E butuh Postgres+server hidup, tidak hermetik/cacheable, lihat `apps/web/e2e/README.md`). Tiga keputusan desain (detail lengkap di README tsb): (1) auth pakai `@clerk/testing`, login Clerk sungguhan sekali lalu reuse `storageState` — bypass flag di kode produksi ditolak karena berisiko keamanan dan mahal dibatalkan; kalau `E2E_CLERK_USER_EMAIL`/`PASSWORD` kosong, spec terautentikasi **skip** (bukan gagal); (2) seeding pakai fixture ber-namespace (`e2e-course-*`, `@e2e.dirakitpro.test`) tanpa reset DB, isolasi antar-test di level course; (3) webhook Midtrans diuji dengan server key milik test sendiri (`E2E_MIDTRANS_SERVER_KEY`, di-inject ke dev server), jadi nol kredensial sandbox asli dibutuhkan untuk menguji transisi state PENDING→PAID/EXPIRED/CANCELLED + idempotency. Diverifikasi jalan: 9 test `anonymous` lolos (webhook + guard redirect), `typecheck`/`lint`/`format:check` bersih. **Belum terverifikasi**: 3 spec `authenticated` (checkout guard, status pembayaran) — Clerk dev instance belum ada user E2E, perlu dibuat manual sekali via `node apps/web/e2e/scripts/clerk-test-user.ts create` lalu isi `.env`; suite tetap skip (hijau) sampai itu dilakukan. **Cakupan Discover/Lesson/Checkpoint/Submission ditunda ke langkah 14** — halaman-halaman itu masih placeholder scaffold, belum ada logika nyata untuk dites; kerangka `test.fixme()` sudah disiapkan di `apps/web/e2e/authenticated/core-journey.todo.e2e.ts` sebagai daftar assertion yang perlu ditulis begitu fitur nyata. **Bug ditemukan (belum diperbaiki, sekadar dicatat)**: (a) `lib/checkout.ts#createCheckoutOrder` insert row `orders` sebelum memanggil Midtrans dan insert `payments` sesudahnya — kalau panggilan Midtrans gagal, order PENDING jadi yatim tanpa payment row, retry berikutnya kena cabang `existingPending` dan poll selamanya karena tidak ada Snap token baru; (b) `app/api/webhooks/midtrans/route.ts` lewat `verifyMidtransSignature` memanggil `Buffer.from(signatureKey, "utf8")` tanpa validasi — payload yang kehilangan field `signature_key` bikin `TypeError` naik jadi 500 alih-alih 400 yang bersih. Keduanya di kode langkah 10 yang sudah merge ke `main`, ditemukan sebagai efek samping menulis test webhook, direkomendasikan diperbaiki sebelum langkah 14. **Catatan lingkungan**: sempat ditemukan `node_modules` di `apps/web` rusak sebagian (66% file terbaca kosong, macOS "Optimize Mac Storage" meng-evict isi file karena folder project ke-sync iCloud dan disk hampir penuh) — diperbaiki dengan `pnpm install --force`, berpotensi terulang kalau ruang disk tidak ditambah atau folder project dikecualikan dari iCloud sync. |
| 14 | Bangun fitur MVP | Implementasi fitur berdasarkan prioritas P0 di PRD, satu fitur/modul per iterasi: implement → review (pakai skill code-review) → test → commit. Jangan mulai fitur P1 sebelum semua P0 selesai. | Fitur berjalan sesuai acceptance criteria PRD | Pending |
| 15 | Deploy staging | Deploy ke Vercel (atau host lain yang kompatibel) di environment staging, pakai kredensial sandbox (Midtrans sandbox, dst) sebelum production. | URL staging bisa diakses | Pending |
| 16 | Review terhadap success metrics | Setelah staging/production jalan, cek balik ke goals & success metrics di PRD §5 — apakah data yang terkumpul (funnel purchase → complete, failure rate checkpoint, dst) bisa diukur sesuai rencana. | Dashboard/report metrics | Pending |

## Catatan perbaikan untuk siklus berikutnya

- Pindahkan langkah **connect git** ke paling awal (langkah 0), bukan setelah semua dokumen selesai, supaya riwayat commit mencerminkan proses berpikir per dokumen (PRD → data model → frontend design), bukan satu commit besar di akhir.
- Pertimbangkan commit terpisah per dokumen besar (PRD, data model, frontend design, README) alih-alih satu commit gabungan, supaya `git log` bisa dipakai sebagai timeline proses desain.
- Tandai versi setiap dokumen besar (PRD sudah pakai versi `V1.4`) supaya perubahan scope bisa dilacak; pertimbangkan pola sama untuk `docs/DATA-MODEL.md` dan `docs/FRONTEND-DESIGN.md` kalau nanti direvisi setelah Fase 0.
